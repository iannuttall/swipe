---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<aside class="surface-muted my-7 px-5 py-5 sm:px-6">
  {title ? <p class="mb-2 font-heading text-base font-semibold">{title}</p> : null}
  <div class="content-prose text-[1rem]">
    <slot />
  </div>
</aside>
