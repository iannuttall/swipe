---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<aside class="my-6 border-2 border-foreground bg-card p-5">
  {title ? <p class="mb-2 font-mono text-sm font-semibold">{title}</p> : null}
  <div class="content-prose">
    <slot />
  </div>
</aside>
