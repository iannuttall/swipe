---
import PixelIcon from "@/components/icons/PixelIcon.astro";
---

{/* Cloned into every [data-code-copy] control on the page. */}
<template data-code-copy-icons>
  <PixelIcon name="copy" class="code-copy-icon size-4" />
  <PixelIcon name="copy-check" class="code-copy-check size-4" />
</template>

<script is:inline>
  (() => {
    const template = document.querySelector("[data-code-copy-icons]");
    if (template) {
      for (const button of document.querySelectorAll("[data-code-copy]")) {
        if (button.childElementCount === 0) {
          button.append(template.content.cloneNode(true));
        }
      }
    }

    if (window.__swipeCodeCopyBound) return;
    window.__swipeCodeCopyBound = true;
    document.addEventListener("click", async (event) => {
      const button = event.target?.closest?.("[data-code-copy]");
      if (!button) return;

      const pre = button.closest(".code-frame")?.querySelector("pre");
      const text = pre?.innerText ?? "";
      if (!text) return;

      try {
        await navigator.clipboard.writeText(text.replace(/\n$/, ""));
      } catch {
        return;
      }

      button.classList.add("copied");
      button.setAttribute("aria-label", "Copied");
      window.setTimeout(() => {
        button.classList.remove("copied");
        button.setAttribute("aria-label", "Copy code");
      }, 1500);
    });
  })();
</script>
